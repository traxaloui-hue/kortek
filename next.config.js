/** @type {import('next').NextConfig} */
module.exports = {
  // Removed experimental.appDir to avoid invalid config warning in Next.js builds.
  // Keep other default settings minimal — you can add safe config flags here.
  reactStrictMode: true
}
